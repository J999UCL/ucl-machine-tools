[project]
name = "ucl-machine-tools"
version = "0.1.0"
description = "UCL CS GPU status, exec, and tmux launch tools"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ucl = "ucl_machine_tools.main_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
