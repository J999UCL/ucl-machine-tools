{
  "schema_version": 1,
  "profiles": {
    "plain-bash": {
      "description": "Run the uploaded script with bash and no environment setup."
    },
    "uv": {
      "description": "Require uv and run the command through uv run.",
      "preflight": [
        {
          "label": "uv is installed",
          "command": "command -v uv >/dev/null"
        }
      ],
      "run_prefix": ["uv", "run", "--"]
    },
    "tsg-pytorch": {
      "description": "Use the UCL TSG Python/CUDA setup and require torch CUDA.",
      "shell": "csh-bootstrap",
      "source": [
        "/opt/Python/Python-3.11.5_Setup.csh"
      ],
      "env": {
        "CUDA_DEVICE_ORDER": "PCI_BUS_ID"
      },
      "preflight_after_setup": [
        {
          "label": "python is available",
          "command": "python3 --version"
        },
        {
          "label": "torch cuda available",
          "command": "python3 - <<'PY'\nimport torch\nprint(torch.__version__, torch.cuda.is_available())\nassert torch.cuda.is_available()\nPY"
        }
      ]
    }
  }
}
